package com.yoyohr

/**
 * <p>UnknowPipeline</p>
 *
 * @author nekoimi 2022/05/29
 */

def build(yamlConf) {
    noticeWarning("Warning！构建流程不支持。")
}

def unitTesting(yamlConf) {
    noticeWarning("Warning！构建流程不支持。")
}

def docker(yamlConf) {
    noticeWarning("Warning！构建流程不支持。")
}

def deploy(yamlConf) {
    noticeWarning("Warning！构建流程不支持。")
}

def testing(yamlConf) {
    noticeWarning("Warning！构建流程不支持。")
}
